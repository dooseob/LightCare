-- ================================================
-- 라이트케어 초기 데이터 삽입
-- Railway 환경 호환성을 위한 data.sql
-- ================================================

-- 이 파일은 schema.sql 실행 후 기본 데이터를 추가하는 용도입니다.
-- 현재는 schema.sql에 INSERT문이 모두 포함되어 있으므로
-- 빈 파일로 두어 오류를 방지합니다.

-- Railway 환경에서 초기화 확인용 메시지
SELECT 'data.sql 파일이 성공적으로 로드되었습니다.' as initialization_status;